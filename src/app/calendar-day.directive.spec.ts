import { CalendarDayDirective } from './calendar-day.directive';

describe('CalendarDayDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarDayDirective();
    expect(directive).toBeTruthy();
  });
});
